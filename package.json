{
  "name": "ikigai-tokenomics",
  "version": "1.0.0",
  "description": "Ikigai Protocol Tokenomics Implementation",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:berachain": "hardhat run scripts/deploy.ts --network berachain"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
